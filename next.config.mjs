/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "hcvjcwyhhdzwduchluiw.supabase.co",
      },
    ],
  },
};

export default nextConfig;
